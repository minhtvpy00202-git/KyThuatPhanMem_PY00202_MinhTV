<%@ page contentType="text/html; charset=UTF-8"%>
<%@ taglib prefix="c" uri="jakarta.tags.core"%>
<%@ taglib prefix="fmt" uri="jakarta.tags.fmt"%>

<div class="container">
  <h2 style="text-align: center; color: var(--primary-color); margin-bottom: 2rem;">📊 Sổ thu chi</h2>
  
  <div class="compact-form">
    <h3 class="form-title">🔍 Lọc theo thời gian</h3>
    <form method="get" action="${pageContext.request.contextPath}/home" class="form-horizontal">
      <div class="form-group">
        <label class="form-label">Từ ngày</label>
        <input type="date" name="from" value="${from}" class="form-control">
      </div>
      <div class="form-group">
        <label class="form-label">Đến ngày</label>
        <input type="date" name="to" value="${to}" class="form-control">
      </div>
      <div class="form-group">
        <button type="submit" class="btn btn-primary">
          🔍 Lọc dữ liệu
        </button>
      </div>
    </form>
  </div>

  <div class="full-width-table">
    <div class="table-header">
      <h3>💸 Chi tiêu</h3>
    </div>
    <div class="table-container">
      <table>
        <thead>
          <tr>
            <th>📅 Ngày</th>
            <th>📂 Danh mục</th>
            <th>💰 Số tiền</th>
            <th>📝 Ghi chú</th>
          </tr>
        </thead>
        <tbody>
          <c:forEach var="t" items="${expense}">
            <tr>
              <td style="font-weight: 600;"><fmt:formatDate value="${t.txDay}" pattern="dd/MM/yyyy"/></td>
              <td>
                <span style="background-color: var(--error-color); color: white; padding: 0.4rem 0.8rem; border-radius: var(--radius-sm); font-size: 0.8rem; font-weight: 600;">
                  ${t.categoryName}
                </span>
              </td>
              <td style="text-align: right; font-weight: 700; color: var(--error-color); font-size: 1.1rem;">
                -<fmt:formatNumber value="${t.amount}" pattern="#,##0.##" /> ₫
              </td>
              <td style="color: var(--text-primary);">${t.notes}</td>
            </tr>
          </c:forEach>
          <c:if test="${empty expense}">
            <tr>
              <td colspan="4" style="text-align: center; color: var(--text-secondary); font-style: italic; padding: 3rem;">
                📭 Chưa có chi tiêu nào trong khoảng thời gian này
              </td>
            </tr>
          </c:if>
        </tbody>
      </table>
    </div>
  </div>

  <div class="full-width-table">
    <div class="table-header">
      <h3>💰 Thu nhập</h3>
    </div>
    <div class="table-container">
      <table>
        <thead>
          <tr>
            <th>📅 Ngày</th>
            <th>📂 Danh mục</th>
            <th>💰 Số tiền</th>
            <th>📝 Ghi chú</th>
          </tr>
        </thead>
        <tbody>
          <c:forEach var="t" items="${income}">
            <tr>
              <td style="font-weight: 600;"><fmt:formatDate value="${t.txDay}" pattern="dd/MM/yyyy"/></td>
              <td>
                <span style="background-color: var(--success-color); color: white; padding: 0.4rem 0.8rem; border-radius: var(--radius-sm); font-size: 0.8rem; font-weight: 600;">
                  ${t.categoryName}
                </span>
              </td>
              <td style="text-align: right; font-weight: 700; color: var(--success-color); font-size: 1.1rem;">
                +<fmt:formatNumber value="${t.amount}" pattern="#,##0.##" /> ₫
              </td>
              <td style="color: var(--text-primary);">${t.notes}</td>
            </tr>
          </c:forEach>
          <c:if test="${empty income}">
            <tr>
              <td colspan="4" style="text-align: center; color: var(--text-secondary); font-style: italic; padding: 3rem;">
                📭 Chưa có thu nhập nào trong khoảng thời gian này
              </td>
            </tr>
          </c:if>
        </tbody>
      </table>
    </div>
  </div>
</div>
